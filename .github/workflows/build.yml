name: Build

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

env:
  GO_VERSION: '1.26'
  GH_TOKEN: ${{ github.token }}

jobs:
  # build Linux
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Build with Docker
        run: |
          docker build -t sq-builder .
          docker create --name sq-temp sq-builder
          docker cp sq-temp:/sq ./sq-linux-amd64
          docker rm sq-temp
          chmod +x sq-linux-amd64

      - name: Test binary
        run: |
          ./sq-linux-amd64 --version || echo "Linux binary test completed"

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: sq-linux-amd64-${{ github.sha }}
          path: sq-linux-amd64

  # build macOS
  build-macos:
    runs-on: macos-latest
    strategy:
      matrix:
        arch: [amd64, arm64]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Build Lua from source
        run: |
          # Download and extract Lua 5.4.7
          curl -L -o lua-5.4.7.tar.gz https://www.lua.org/ftp/lua-5.4.7.tar.gz
          tar -xzf lua-5.4.7.tar.gz
          cd lua-5.4.7

          # Set architecture-specific compiler flags
          if [ "${{ matrix.arch }}" = "amd64" ]; then
            export ARCH_FLAGS="-arch x86_64"
            export LUA_PREFIX="/tmp/lua-x86_64"
          else
            export ARCH_FLAGS="-arch arm64"
            export LUA_PREFIX="/tmp/lua-arm64"
          fi

          # Build Lua with specific architecture
          make clean || true
          make CC="clang ${ARCH_FLAGS}" AR="ar rcu" RANLIB="ranlib" macosx
          make install INSTALL_TOP="${LUA_PREFIX}"

          # Create pkg-config file for our custom Lua build
          mkdir -p "${LUA_PREFIX}/lib/pkgconfig"
          cat > "${LUA_PREFIX}/lib/pkgconfig/lua5.4.pc" << EOF
          prefix=${LUA_PREFIX}
          exec_prefix=\${prefix}
          libdir=\${exec_prefix}/lib
          includedir=\${prefix}/include

          Name: Lua
          Description: An Extensible Extension Language
          Version: 5.4.7
          Requires:
          Libs: -L\${libdir} -llua -lm
          Cflags: -I\${includedir}
          EOF

          # Verify the build
          file "${LUA_PREFIX}/bin/lua"
          "${LUA_PREFIX}/bin/lua" -v

      - name: Build macOS binary
        env:
          GOOS: darwin
          GOARCH: ${{ matrix.arch }}
          CGO_ENABLED: 1
        run: |
          # Set architecture-specific paths
          if [ "${{ matrix.arch }}" = "amd64" ]; then
            export LUA_PREFIX="/tmp/lua-x86_64"
            export ARCH_FLAGS="-arch x86_64"
          else
            export LUA_PREFIX="/tmp/lua-arm64"
            export ARCH_FLAGS="-arch arm64"
          fi

          # Set PKG_CONFIG_PATH to find our custom Lua
          export PKG_CONFIG_PATH="${LUA_PREFIX}/lib/pkgconfig:$PKG_CONFIG_PATH"

          # Set CGO flags for static linking
          export CGO_CFLAGS="-I${LUA_PREFIX}/include ${ARCH_FLAGS}"
          export CGO_LDFLAGS="${ARCH_FLAGS} -L${LUA_PREFIX}/lib -llua -lm -framework CoreMIDI -framework CoreFoundation"

          # Verify pkg-config can find our Lua
          pkg-config --exists lua5.4 && echo "pkg-config found lua5.4" || echo "pkg-config failed to find lua5.4"
          pkg-config --cflags lua5.4 || echo "Failed to get cflags"
          pkg-config --libs lua5.4 || echo "Failed to get libs"

          # Build the binary
          go build -tags lua54 -ldflags "-s -w" -o sq-darwin-${{ matrix.arch }}

          # Verify the binary architecture
          file sq-darwin-${{ matrix.arch }}
          lipo -info sq-darwin-${{ matrix.arch }}

      - name: Verify binary
        run: |
          file sq-darwin-${{ matrix.arch }}
          otool -L sq-darwin-${{ matrix.arch }} || true

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: sq-darwin-${{ matrix.arch }}-${{ github.sha }}
          path: sq-darwin-${{ matrix.arch }}

  release:
    needs: [build-linux, build-macos]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          merge-multiple: true

      - name: Verify artifacts
        run: |
          ls -la sq-*
          file sq-* || true

      - name: Create release directories
        run: |
          mkdir -p output/sq-linux-X86_64/bin
          mkdir -p output/sq-darwin-X86_64/bin
          mkdir -p output/sq-darwin-arm64/bin

          mv sq-linux-amd64 output/sq-linux-X86_64/bin/sq
          mv sq-darwin-amd64 output/sq-darwin-X86_64/bin/sq
          mv sq-darwin-arm64 output/sq-darwin-arm64/bin/sq
          #
          # Make them executable
          chmod +x output/*/bin/sq

          tar -cvzf sq-linux-X86_64.tar.gz -C output sq-linux-X86_64
          tar -cvzf sq-darwin-X86_64.tar.gz -C output sq-darwin-X86_64
          tar -cvzf sq-darwin-arm64.tar.gz -C output sq-darwin-arm64

          ./changelog.sh WITHINSTALL > release-notes.md
          # if the github_ref has alpha or beta make this a prerelease
          case "${GITHUB_REF}" in
            *alpha*|*beta*)
              gh release create --prerelease -F release-notes.md "${GITHUB_REF}" -- *.tar.gz sq-manual.pdf
              ;;
            *)
              gh release create -F release-notes.md "${GITHUB_REF}" -- *.tar.gz sq-manual.pdf
              ;;
          esac
